<Window x:Class="ControlBrazoRobotico.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmación" Height="180" Width="350"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize">

    <Border Background="#1E1E1E" CornerRadius="12" BorderBrush="#FF6D00" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtMensaje" Text="¿Deseas eliminar esta rutina?" 
                       Foreground="White" FontSize="14" TextWrapping="Wrap" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                <Button x:Name="btnAceptar" Content="SÍ, ELIMINAR" Click="BtnSi_Click" Width="110" Height="30" Style="{StaticResource BtnPlay}" FontWeight="Bold" Margin="0,0,10,0"/>

                <Button x:Name="btnCancelar" Content="CANCELAR" Click="BtnNo_Click" Width="110" Height="30" 
                        Background="#444" Foreground="White" Margin="10,0,0,0" Style="{StaticResource BtnClear}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>