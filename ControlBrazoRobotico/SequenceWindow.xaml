<Window x:Class="ControlBrazoRobotico.SequenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Programador de Secuencias" Height="550" Width="850"
        Background="#121212" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="BtnEstiloBase" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#222"/>
                                <Setter Property="Foreground" Value="#555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnPlay" TargetType="Button" BasedOn="{StaticResource BtnEstiloBase}">
            <Setter Property="Background" Value="#FF6D00"/>
        </Style>
        <Style x:Key="BtnStop" TargetType="Button" BasedOn="{StaticResource BtnEstiloBase}">
            <Setter Property="Background" Value="#B43232"/>
        </Style>
        <Style x:Key="BtnClear" TargetType="Button" BasedOn="{StaticResource BtnEstiloBase}">
            <Setter Property="Background" Value="#444"/>
        </Style>
        <Style x:Key="BtnSave" TargetType="Button" BasedOn="{StaticResource BtnEstiloBase}">
            <Setter Property="Background" Value="#2ECC71"/>
            <Setter Property="Height" Value="38"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="160"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="SECUENCIA DE MOVIMIENTOS" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="RETRASO (MS):" Foreground="#888" VerticalAlignment="Center" FontSize="10" Margin="30,0,10,0"/>

            <TextBox x:Name="txtDelay" Text="1500" Width="60" Height="28" 
                     Background="#222" Foreground="White" VerticalContentAlignment="Center" 
                     HorizontalContentAlignment="Center" BorderBrush="#444">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <Button x:Name="btnPlay" Content="REPRODUCIR" Style="{StaticResource BtnPlay}" Width="120" Margin="20,0,0,0" Click="BtnPlay_Click"/>
            <Button x:Name="btnStop" Content="DETENER" Style="{StaticResource BtnStop}" Width="85" Margin="10,0,0,0" Click="BtnStop_Click" IsEnabled="False"/>
            <Button x:Name="btnLimpiar" Content="LIMPIAR" Style="{StaticResource BtnClear}" Width="80" Margin="10,0,0,0" Click="BtnClear_Click"/>
        </StackPanel>

        <Border Grid.Row="1" Grid.Column="0" Background="#1E1E1E" CornerRadius="10" BorderBrush="#333" BorderThickness="2" AllowDrop="True" DragEnter="Timeline_DragEnter" DragLeave="Timeline_DragLeave" DragOver="Timeline_DragOver" Drop="Timeline_Drop">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <WrapPanel x:Name="panelTimeline" Orientation="Horizontal" Margin="5"/>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Grid.RowSpan="3" Margin="15,0,0,0" Background="#181818" CornerRadius="10" Padding="12">
            <StackPanel>
                <Button Content="GUARDAR ESTA RUTINA" Style="{StaticResource BtnSave}" Margin="0,0,0,15" Click="BtnGuardarRutina_Click"/>
                <TextBlock Text="RUTINAS GUARDADAS" Foreground="#555" FontSize="10" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300">
                    <StackPanel x:Name="panelRutinasSaved"/>
                </ScrollViewer>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="⬇ ARRASTRA LAS POSICIONES ABAJO PARA EDITAR LA SECUENCIA ⬇" 
                   Foreground="#444" FontSize="10" Margin="0,15" HorizontalAlignment="Center" FontWeight="Bold"/>

        <Border Grid.Row="3" Grid.Column="0" Background="#181818" CornerRadius="10" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <WrapPanel x:Name="panelLibrary" Orientation="Horizontal"/>
            </ScrollViewer>
        </Border>

    </Grid>
</Window>